let numeroSorteado = Math.floor(Math.random() * 100) + 1;
    console.log("Numero sorteado "+numeroSorteado);
    let botao = document.getElementById("meuBotao");
    let botao2 = (document.getElementById("meuBotao2").value);

    let tentativasRestantes = 10;

    function recomecar() {
     
        document.getElementById("mensagem") = 'A pagina vai recarregar agora...';
        

        location.reload();
      }
   


function adivinharNumero(){
    event.preventDefault();


    


  
    let chute = Number(document.getElementById("chute").value);
    let mensagem = (document.getElementById("mensagem").value);
    let botao = (document.getElementById("meuBotao2").value);


     tentativasRestantes--;

  
    

     if(tentativasRestantes <= 0){
        document.getElementById('mensagem').innerText = "Suas tentativas acabaram!O numero era: "+numeroSorteado+" Aperte o botao recomecar na página para reiniciar.";
        botao.disabled = true;
       
        
    }

if( chute === numeroSorteado){
        document.getElementById('mensagem').innerText = "Parabems voce acertou ";
        botao.disabled = true;
        
    
    }else if( chute < numeroSorteado ){
        document.getElementById('mensagem').innerText = "Voce errou, chute mais alto (tentativas restantes = "+ tentativasRestantes+ ")";
        
    }else{
        document.getElementById('mensagem').innerText = "Voce errou, chute mais baixo (tentativas restantes = "+ tentativasRestantes+ ")";
    }
    if(tentativasRestantes <= 0 && chute !== numeroSorteado){
        document.getElementById('mensagem').innerText = "Suas tentativas acabaram! O número era " + numeroSorteado;

        botao.disabled = true;
        
       
    }
}

